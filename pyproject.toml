[project]
name = "openh-rf"
version = "0.0.1"
description = "Tooling and examples for the OpenH-RF open ultrasound channel-capture dataset initiative."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [
    { name = "OpenH-RF Contributors" },
]
dependencies = [
    # Point-based (coordinates) data spec — tue-bmd/zea@openh-rf, pinned to a SHA
    # for reproducibility. Update the SHA when the openh-rf spec advances.
    "zea @ git+https://github.com/tue-bmd/zea.git@870e322258cbfb801165c8fe25c5cfe2c21e9765",
    "numpy",
    "huggingface_hub",
    "datasets",
    "matplotlib",
    "jax",
]

[project.optional-dependencies]
test = ["pytest"]
gpu = ["jax[cuda12]"]
torch = ["torch"]
tf = ["tensorflow"]
tf-gpu = ["tensorflow[and-cuda]"]

[project.urls]
Homepage = "https://github.com/open-h/OpenH-RF"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []

[tool.pytest.ini_options]
markers = [
    "heavy: marks tests requiring network, GPU, or large downloads (deselect with '-m \"not heavy\"')",
]
